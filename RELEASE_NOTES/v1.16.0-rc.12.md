# v1.16.0-rc.12 — three more enrichers

§13.8 enrichers: dns-passive-reputation + stylometric-supply-chain + threatintel-IP. Each paired with idempotency test per Pass 2026-C.

- `com.maccrab.enricher.dns-passive-reputation` — heuristic: suspicious TLD + homoglyph (Cyrillic look-alikes) + mixed-script + brand impersonation patterns.
- `com.maccrab.enricher.stylometric-supply-chain` — heuristic: eval / exec / atob markers + long base64 / hex blob detection.
- `com.maccrab.enricher.threatintel-ip` — mirror of threatintel-domain for IP addresses; static IOC set.

1812 / 337 suites passing. All four 2026-series audit passes green.

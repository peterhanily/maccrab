# v1.13.0-rc.5 — v1.13a substrate feature-complete

**Status: internal release candidate.** Not published to Sparkle appcast or Homebrew cask. Source tag only.

Closes the fifth and final sub-slice of v1.13a. With this RC the entire Mac Context Plugin Platform substrate is feature-complete and audit-gated. v1.13b operator surface work begins next.

## What landed in v1.13a-5

**Pass 2026-D in pre-release-audit.sh.** Source-tree enforcement that:
- `Sources/MacCrabForensics/Storage/ArtifactStore.swift` still contains the `plaintextCaseRejectsNonMetadata` runtime gate.
- `Tests/MacCrabForensicsTests/ArtifactStoreTests.swift` covers each of the four non-metadata classes (`content` / `personalComms` / `credentialAdjacent` / `secret`) with explicit reject-at-INSERT assertions.

The runtime invariant landed in v1.13a-1 (commit `c57a4a2`); v1.13a-5 closes the loop by ensuring the audit can't ship without the invariant.

**Snapshot-isolation fixture.** New `LiveDBSnapshotTests` case proves the snapshot-before-parse contract empirically: take a snapshot, write a new row into the source, verify the snapshot still reflects pre-write state. TCC-lite and the future BAM collector both depend on this property.

## Substrate status at v1.13a complete

| Sub-slice | What it delivered | Status |
|---|---|---|
| **v1.13a-1** | CSQLCipher vendor; MacCrabForensics SPM target + plugin protocol; encrypted ArtifactStore with Pass 2026-D INSERT gate; Case model + login-keychain DEK + BlobVault; maccrabctl case/plugin subcommands; fixture plugin; Pass 2026-A + 2026-B. | Shipped v1.13.0-rc.1 |
| **v1.13a-2** | codesign-resolve enricher; PluginRunner.runEnricher path; Pass 2026-C. | Shipped v1.13.0-rc.2 |
| **v1.13a-3** | TCC-lite collector; LiveDBSnapshot utility; deterministic TCC risk scoring; service normalization. | Shipped v1.13.0-rc.3 |
| **v1.13a-4** | launchd-lite collector + plist parser; codesign cross-reference on every launchd entry. | Shipped v1.13.0-rc.4 |
| **v1.13a-5** | Pass 2026-D source-tree enforcement; snapshot-isolation fixture. | **This RC** |

## Four audit passes — v1.13a arc complete

`scripts/pre-release-audit.sh` now enforces:

- **Pass 2026-A** — plugin manifest integrity (registered, namespaced, every output declares privacyClass).
- **Pass 2026-B** — single ArtifactStore writer (no other file may INSERT into the three tables).
- **Pass 2026-C** — enricher idempotency coverage (every Enricher has a paired idempotency test).
- **Pass 2026-D** — privacy_class consistency + plaintext rejects non-metadata at INSERT time.

## Three v1.13 plan questions — answered

Per plan §9.5.1:

1. **What apps have FDA / Accessibility / Automation / Screen Recording / Mic / Camera / AppleEvents?** ✓ TCC-lite (v1.13a-3) catalogs every grant with deterministic risk scoring.
2. **What persistence exists on this Mac, and what is unsigned, unfamiliar-team-signed, or recently modified?** ✓ launchd-lite (v1.13a-4) inventories every plist + cross-references codesign posture per entry.
3. **Which recent Track 1 alerts involve unsigned or unfamiliar-team-signed processes?** ✓ codesign-resolve enricher (v1.13a-2) makes the fields available for Sigma rule matching; full per-event integration is v1.13b territory.

## Tests

1746 across 323 suites, all passing. 78 MacCrabForensics tests across plugin platform / storage / cases / collectors / enrichers / snapshot.

## Distribution

This RC does NOT publish to Sparkle appcast or Homebrew cask. Source tag only. The public channels stay on 1.12.9 throughout the v1.13a → v1.15 arc.

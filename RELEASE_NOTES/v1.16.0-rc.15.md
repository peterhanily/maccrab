# v1.16.0-rc.15 — FaceTime + Biome discovery

**`com.maccrab.forensics.facetime`** — reads `~/Library/Application Support/CallHistoryDB/CallHistory.storedata` (snapshot-before-parse). Emits `facetime.call` per ZCALLRECORD row: peer_address + date + duration + answered + service_provider + originated_by_user. Plan §13.4 (deprioritised). Privacy class `personalComms`.

**`com.maccrab.forensics.biome`** — discovery for macOS 13+'s Biome event streams under `~/Library/Biome/Streams/public/`. Emits `biome.stream` per stream directory with file_count + total_bytes. Full record parsing (Apple-internal BiomeEvent binary format) deferred.

Internal RC. 24 tags total.

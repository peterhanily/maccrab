name: Release

on:
  push:
    tags: ['v*']

jobs:
  release:
    runs-on: macos-14
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install PyYAML
        run: pip install pyyaml

      - name: Build release binary
        run: swift build -c release 2>&1 || echo "Binary build requires ES entitlement - packaging rules only"

      - name: Compile rules
        run: |
          mkdir -p compiled_rules
          python3 Compiler/compile_rules.py --input-dir Rules/ --output-dir compiled_rules/

      - name: Package
        run: |
          mkdir -p dist/hawkeye
          cp .build/release/hawkeyed dist/hawkeye/ 2>/dev/null || true
          cp .build/release/hawkctl dist/hawkeye/ 2>/dev/null || true
          cp -r compiled_rules dist/hawkeye/
          cp -r Rules dist/hawkeye/
          cp -r Compiler dist/hawkeye/
          cp README.md LICENSE CONTRIBUTING.md dist/hawkeye/
          cd dist && tar czf hawkeye-${{ github.ref_name }}-macos-arm64.tar.gz hawkeye/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/hawkeye-*.tar.gz
          generate_release_notes: true
